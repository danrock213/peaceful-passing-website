export interface VendorInteractionsProps {
  vendorId: string;
}

export default function VendorInteractions({ vendorId }: VendorInteractionsProps) {
  // Use vendorId to load messages or vendor data
}
